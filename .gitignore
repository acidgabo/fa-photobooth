# Dependencias
node_modules/

# Variables de entorno reales (credenciales de NetPay, rutas de la máquina, etc.)
# — .env.example sí se sube, .env NUNCA.
.env

# Fotos capturadas en modo direct/windirect (contenido generado en runtime)
captures/

# Estado operativo en runtime (folios pendientes por reversar, etc.)
data/

# Logs
npm-debug.log*
*.log

# Sistema operativo
.DS_Store
Thumbs.db

# Scripts de prueba manual aislada contra NetPay (test-netpay-token.js dispara
# solo getAccessToken(); test-netpay-venta.js SÍ dispara un cobro real de
# $1.00 MXN en la terminal física) — se mantienen solo locales, nunca se suben.
test-netpay-token.js
test-netpay-venta.js
